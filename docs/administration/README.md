# Administration

This chapter contains information useful for people wanting to host or already hosting their own nbsp instance.

## Deployment

*TODO: Write out proper details for deploying nbsp.*

You can also find out how [nbsp.party](https://nbsp.party) is deployed in [the nbsp.party repository](https://github.com/bearbearean/nbsp.party). It contains a set of Ansible playbooks designed to take a fresh Debian 13 server from zero to running nbsp.

## Configuration

From the beginning of the nbsp development, instance-wide configuration has been considered through the use of a database table called `nbsp_config`.

Read more about configuring your nbsp instance in [the Configuration section](./configuration/index.html).
